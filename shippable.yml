# language setting
language: node_js

# version numbers
node_js:
  - "0.11"
  - "0.10"
addons:
  firefox: "23.0"
services:
  - selenium
script:
  - "export DISPLAY=:99.0"
  - xvfb-run --server-args="-ac" npm test
