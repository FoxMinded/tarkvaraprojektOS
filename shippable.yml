# language setting
language: node_js

# version numbers
node_js:
  - "0.11"
  - "0.10"

services:
  - selenium
addons:
  firefox: "17.0"
script:
  - export DISPLAY=:99.0
  - xvfb-run --server-args="-ac" npm test
