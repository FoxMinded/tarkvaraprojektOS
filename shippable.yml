# language setting
build_image: drydock/u14nodall:prod
language: node_js

# version numbers
node_js:
  - "0.11"
  - "0.10"

services:
  - selenium
addons:
  firefox: "23.0"
script:
  - export DISPLAY=:99.0
  - xvfb-run --server-args="-ac" npm test
